{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch Grunt",
            "args": ["run"],
            "program": "${workspaceFolder}/node_modules/.bin/grunt",
            "stopOnEntry": true,
            "cwd": "${workspaceRoot}"
        }
    ]
}